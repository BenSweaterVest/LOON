# LOON Production Environment Variables
# Copy this to .env.production and fill in production values

# GitHub Configuration (Required)
# Your GitHub repository in owner/repo format
# Example: YOUR_GITHUB_ORG/your-loon-repo
GITHUB_REPO=YOUR_GITHUB_ORG/your-loon-repo

# GitHub Personal Access Token (Required)
# Must have 'repo' scope for contents read/write
# Create at: https://github.com/settings/tokens?type=beta
# Scopes needed: Contents (read/write)
# WARNING: Treat this as a secret - never commit to version control
GITHUB_TOKEN=github_pat_xxxxxxxxxxxxxxx

# One-time initial setup token (Required until first admin is created)
# Use a long random secret. Remove or rotate after initial setup.
SETUP_TOKEN=replace-with-strong-random-token

# Cloudflare Configuration (Production)
# Set these in Cloudflare Pages dashboard: Settings > Environment variables > Production

# KV Namespace Binding (Required for data storage)
# Configure KV binding in Cloudflare Pages: Settings > Functions > KV namespace bindings
# Binding name: LOON_DB (preferred) or KV (compatibility fallback)
# Namespace: Select your KV namespace from dropdown
# Optional local Wrangler automation can store IDs in wrangler.local.toml / wrangler.toml
KV_NAMESPACE_ID=your-kv-namespace-id

# Passkeys/WebAuthn configuration (recommended if using passkeys in production)
# RP_ID should match your production host.
# RP_ORIGIN must be full origin for the same host.
# Example:
# RP_ID=your-project.pages.dev
# RP_ORIGIN=https://your-project.pages.dev
RP_ID=your-project.pages.dev
RP_ORIGIN=https://your-project.pages.dev

# Cloudflare Account ID (Optional, for Images API)
# Find at: Cloudflare Dashboard > Account home > API Token (bottom left)
# Required only if you enable image uploads
CF_ACCOUNT_ID=your-account-id

# Cloudflare Images API Token (Optional, for image uploads)
# Create at: Cloudflare Dashboard > API Tokens
# Scopes: Account Resources > Cloudflare Images > Edit
# Required only if you enable image uploads
CF_IMAGES_TOKEN=your-images-token

# Application Configuration
# Set in Cloudflare Pages settings
ENVIRONMENT=production

# Initial Admin Setup (One-time only)
# 1) Deploy with SETUP_TOKEN configured
# 2) Open /admin.html
# 3) Complete Initial Setup form with SETUP_TOKEN, username, and password
# 4) Remove or rotate SETUP_TOKEN after setup is complete
