# Cloudflare Pages custom headers
# https://developers.cloudflare.com/pages/configuration/headers/
#
# SECURITY OVERVIEW:
# These headers provide defense-in-depth against common web vulnerabilities:
# - X-Frame-Options: Prevents clickjacking attacks
# - X-Content-Type-Options: Prevents MIME-type sniffing
# - Content-Security-Policy: Controls resource loading to prevent XSS
# - Referrer-Policy: Controls information sent in Referer header
# - Permissions-Policy: Restricts browser feature access

# Security headers for all pages
/*
  X-Frame-Options: DENY
  X-Content-Type-Options: nosniff
  Referrer-Policy: strict-origin-when-cross-origin
  Permissions-Policy: camera=(), microphone=(), geolocation=()
  # Content-Security-Policy explanation:
  # - default-src 'self': Only load resources from same origin by default
  # - script-src 'self' 'unsafe-inline': Allow inline scripts (needed for admin UI)
  # - style-src 'self' 'unsafe-inline' cdn.jsdelivr.net: Allow Pico CSS from CDN
  # - img-src 'self' data: https:: Allow images from self, data URIs, and HTTPS
  # - connect-src 'self' api.github.com: Allow API calls to self and GitHub
  # - font-src 'self': Only load fonts from same origin
  # - frame-ancestors 'none': Prevent embedding in iframes (same as X-Frame-Options)
  Content-Security-Policy: default-src 'self'; script-src 'self' 'unsafe-inline'; style-src 'self' 'unsafe-inline' https://cdn.jsdelivr.net; img-src 'self' data: https:; connect-src 'self' https://api.github.com; font-src 'self'; frame-ancestors 'none'

# Cache control for static assets
# Long cache with immutable flag for versioned resources
/*.css
  Cache-Control: public, max-age=31536000, immutable

/*.js
  Cache-Control: public, max-age=31536000, immutable

# Don't cache JSON content (it changes frequently via GitHub commits)
/data/*
  Cache-Control: no-cache, must-revalidate

# Admin pages should not be cached and should not be indexed
/admin.html
  Cache-Control: no-store
  X-Robots-Tag: noindex

/admin-v2.html
  Cache-Control: no-store
  X-Robots-Tag: noindex
